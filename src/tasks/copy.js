'use strict';

import path from 'path';
import fs from 'fs';
import colors from 'colors';

class Copy {
	constructor(src, dest) {
		this.src = src;
		this.dest = dest;

		this.boot();
	}

	boot() {
		// fs.renameSync
	}
}